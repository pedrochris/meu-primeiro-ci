name: CI pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
 build-and-test:
   runs-on: ubuntu-latest
   steps:
   - name: baixar codigo
   uses: actions/chackout@v4

   name: instalar dependencias
   run: npm install
   name:rodas testes
   run: npm test
